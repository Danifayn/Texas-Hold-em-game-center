{
  "name": "functions",
  "description": "Cloud Functions for Firebase",
  "dependencies": {
    "firebase-admin": "^4.1.2",
    "firebase-functions": "^0.5",
    "object.assign": "^4.0.4"
  },
  "private": true,
  "scripts": {
    "functions": "tsc ./src/index.ts --outDir ./build && firebase deploy --only functions"
  }
}
