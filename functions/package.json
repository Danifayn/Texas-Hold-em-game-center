{
  "name": "functions",
  "description": "Cloud Functions for Firebase",
  "dependencies": {
    "@google-cloud/debug-agent": "^1.0.0",
    "firebase-admin": "^4.2.1",
    "firebase-functions": "^0.5.7",
    "firebase-tools": "^2.2.1",
    "object.assign": "^4.0.4",
    "poker-evaluator": "^0.3.1"
  },
  "private": true,
  "scripts": {
    "build": "rd /s /q \"./build\" && tsc --outDir ./build",
    "test": "npm run build && jasmine",
    "functions": "npm run build && firebase deploy --only functions",
    "functions2": "npm run build && firebase deploy --only functions:register"
  },
  "devDependencies": {
    "jasmine": "^2.5.3"
  }
}
